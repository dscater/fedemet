<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0 text-dark">Tratamientos</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="{{urlglobal}}/home">Inicio</a></li>
                    {{#if_equal user.tipo "DOCTOR"}}
                    <li class="breadcrumb-item"><a href="{{urlglobal}}/seguimientos">Seguimientos</a></li>
                    {{/if_equal}}
                    <li class="breadcrumb-item active">Tratamientos</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<section class="content">
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="card">
                    <div class="card-header">
                        <div class="col-md-8 col-md-offset-2"> 
                            <table class="table table-bordered">
                                <tr>
                                    <td width="120px">Paciente: </td>
                                    <td>{{seguimiento.paciente.nombre}} {{seguimiento.paciente.paterno}} {{seguimiento.paciente.materno}}</td>
                                    <td rowspan="3" class="foto_user">
                                        <img src="{{urlglobal}}/imgs/users/{{seguimiento.paciente.user.foto}}" alt="Foto">
                                    </td>
                                </tr>
                                <tr>
                                    <td>C.I.:</td>
                                    <td>{{seguimiento.paciente.ci}} {{seguimiento.paciente.ci_exp}}</td>
                                </tr>
                                <tr>
                                    <td>Teléfono - Celular</td>
                                    <td>{{seguimiento.paciente.fono}} - {{seguimiento.paciente.cel}}</td>
                                </tr>
                            </table>
                        </div>
                        {{#if_equal user.tipo "DOCTOR"}}
                        <a href="{{urlglobal}}/tratamientos/create/{{seguimiento.id}}" class="btn btn-info"><i class="fa fa-plus"></i> Nuevo</a>
                        {{/if_equal}}
                    </div>
                    <!-- /.card-header -->
                    <div class="card-body">
                        <table id="example2" class="table data-table table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>Fecha</th>
                                    <th>Hora</th>
                                    <th>Subjetivo</th>
                                    <th>Objetivo</th>
                                    <th>Diagnostico</th>
                                    <th>Tratamiento</th>
                                    <th>Edad</th>
                                    <th>Peso</th>
                                    <th>Temperatura</th>
                                    <th>Opciones</th>
                                </tr>
                            </thead>
                            <tbody>
                                {{#each tratamientos}}
                                <tr>
                                    <td>{{this.fecha}}</td>
                                    <td>{{this.hora}}</td>
                                    <td>{{this.subjetivo}}</td>
                                    <td>{{this.objetivo}}</td>
                                    <td>{{this.diagnostico}}</td>
                                    <td>{{this.tratamiento}}</td>
                                    <td>{{this.edad}}</td>
                                    <td>{{this.peso}}</td>
                                    <td>{{this.temperatura}}</td>
                                    <td class="btns-opciones">
                                        {{#if_equal ../user.tipo "DOCTOR"}}
                                        <a href="{{urlglobal}}/tratamientos/edit/{{this.id}}" class="modificar"><i class="fa fa-edit" data-toggle="tooltip" data-placement="left" title="Modificar"></i></a>
    
                                        <a href="#" data-url="{{urlglobal}}/tratamientos/destroy/{{this.id}}" data-toggle="modal" data-target="#modal-eliminar" class="eliminar"><i class="fa fa-trash" data-toggle="tooltip" data-placement="left" title="Eliminar"></i></a>
                                        {{/if_equal}}
                                    </td>
                                </tr>
                                {{/each}}
                            </tbody>
                        </table>
                    </div>
                    <!-- /.card-body -->
                </div>
              <!-- /.card -->
            </div>
            <!-- /.col -->
        </div>
        <!-- /.row -->
    </div>
</section>

{{>modal/eliminar}}

{{#section 'scripts'}}
<script>
     $('table.data-table').DataTable({
        order:[
            [0,'desc']
        ],
        columns : [
            {width:"10%"},
            null,
            null,
            null,
            null,
            null,
            null,
            null,
            {width:"5%"},
            {width:"10%"},
        ],
        scrollCollapse: true,
        language: lenguaje,
        pageLength:25
    });

 
    // ELIMINAR
    $(document).on('click','table tbody tr td.btns-opciones a.eliminar',function(e){
        e.preventDefault();
        let tratamiento = $(this).parents('tr').children('td').eq(0).text();
        $('#mensajeEliminar').html(`¿Está seguro(a) de eliminar el tratamiento con fecha <b>${tratamiento}</b>?<br><h4>Esta acción no se podra deshacer.</h4>`);
        let url = $(this).attr('data-url');
        console.log($(this).attr('data-url'));
        $('#formEliminar').prop('action',url);
    });

    $('#btnEliminar').click(function(){
        $('#formEliminar').submit();
    });

</script>
{{/section}}